import 'package:flutter/material.dart';

class AccidentesStatsView extends StatelessWidget {
  const AccidentesStatsView({super.key});
  @override
  Widget build(BuildContext context) =>
      Scaffold(appBar: AppBar(title: const Text('Estadísticas')));
}
