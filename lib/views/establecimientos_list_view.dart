import 'package:flutter/material.dart';

class EstablecimientosListView extends StatelessWidget {
  const EstablecimientosListView({super.key});
  @override
  Widget build(BuildContext context) =>
      Scaffold(appBar: AppBar(title: const Text('Establecimientos')));
}
